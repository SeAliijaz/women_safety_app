import 'package:flutter/material.dart';

class RatingAppScreen extends StatelessWidget {
  const RatingAppScreen({Key? key}) : super(key: key);
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('RatingAppScreen'),
      ),
      body: Center(
        child: Text('RatingAppScreen'),
      ),
    );
  }
}
